name: Veracode Analysis

on:
  workflow_call: # Permite ser chamado após o build
    inputs:
      build-id:
        required: true
        type: string

jobs:
  environment-check:
    runs-on: ubuntu-22.04
    steps:
      - name: Verificar Ambiente
        run: |
          echo "🛠️ Verificando ambiente..."
          uname -a
          echo "✅ Ambiente pronto."
        
  autopack:
    needs: environment-check
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Baixar e Instalar CLI do Veracode
        run: |
          echo "📥 Baixando e instalando o CLI do Veracode..."
          curl -fsS https://tools.veracode.com/veracode-cli/install | sh

      - name: Empacotar Artefatos com o CLI do Veracode
        run: |
        
          BUILD_ID="${{ inputs.build-id }}"

          # Diretório-base do repositório
          REPO_DIR="${{ github.workspace }}"

          # Diretório final de artefatos, contendo o build-id
          OUTPUT_DIR="$REPO_DIR/artifacts/${BUILD_ID}"
          
          # Caminho do ZIP final
          FINAL_ZIP="$OUTPUT_DIR/analysisPack.zip"
          
          # Criar o diretório de artefatos (com subpasta do build-id)
          mkdir -p "$OUTPUT_DIR"
          echo "📂 Diretório de artefatos definido como: $OUTPUT_DIR"
          
          # Empacotar com o CLI do Veracode
          echo "📦 Empacotando artefatos do diretório $REPO_DIR..."
          ./veracode package --source "$REPO_DIR" --output "$OUTPUT_DIR" --trust
          
          # Descompactar qualquer .zip intermediário que o CLI possa ter gerado
          find "$OUTPUT_DIR" -name "*.zip" | while read filename; do
            echo "Descompactando $filename..."
            unzip -o "$filename" -d "$(dirname "$filename")"
          done
          
          # Remover os zips intermediários (se houver)
          rm -rf "$OUTPUT_DIR"/*.zip 
          
          # Consolidar todos os arquivos em um único ZIP
          echo "📂 Consolidando artefatos em um único arquivo ZIP: $FINAL_ZIP..."
          cd "$OUTPUT_DIR"
          zip -r "$FINAL_ZIP" ./*
          
          # Validar se o arquivo final existe e não está vazio
          if [ ! -f "$FINAL_ZIP" ] || [ ! -s "$FINAL_ZIP" ]; then
            echo "❌ Arquivo inválido ou vazio: $FINAL_ZIP. Abortando."
            exit 1
          fi

          echo "✅ Pacote consolidado gerado com sucesso: $FINAL_ZIP"

      - name: Publicar Artefato
        uses: actions/upload-artifact@v3
        with:
          # Mesmo nome que será baixado nos outros jobs
          name: analysisPack
          path: ${{ github.workspace }}/artifacts/analysisPack.zip

  Veracode_SCA:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Veracode SCA
        env:
          SRCCLR_API_TOKEN: ${{ secrets.SRCCLR_API_TOKEN }} 
        run: |
          curl -sSL 'https://download.sourceclear.com/ci.sh' | bash -s – scan --update-advisor --pull-request --allow-dirty
  
  veracode-sast-pipeline_scan:
    needs: autopack
    runs-on: ubuntu-latest
    name: veracode pipeline scan

    steps:
      - name: checkout repo
        uses: actions/checkout@v4
      
      - name: get archive
        uses: actions/download-artifact@v3
        with:
          name: analysisPack  # Mesmo nome do artifact publicado
          path: .  # Baixa para o diretório atual

      - name: veracode pipeline-scan
        id: pipeline-scan
        uses: veracode/Veracode-pipeline-scan-action@v1.0.15
        with:
          vid: ${{ secrets.VERACODE_API_ID }}
          vkey: ${{ secrets.VERACODE_API_KEY }}
          file: './analysisPack.zip'  # Arquivo que baixamos

  veracode-sast-policy-scan:
    needs: [ autopack, veracode-sast-pipeline_scan ]
    runs-on: ubuntu-22.04
    steps:
      - name: Baixar Artefato
        uses: actions/download-artifact@v3
        with:
          name: analysisPack
          path: .  # Baixa novamente no diretório atual

      - name: Veracode SAST Policy Scan
        uses: veracode/veracode-uploadandscan-action@0.2.6
        with:
          appname: ${{ github.repository }}
          createprofile: true
          filepath: './analysisPack.zip'
          vid: ${{ secrets.VERACODE_API_ID }}
          vkey: ${{ secrets.VERACODE_API_KEY }}
          version: "${{ github.run_id }}"
          scantimeout: 12
